server:
  port: 8888

spring:
  application:
    name: gateway-service
  profiles:
    active: dev
  config:
    import: optional:nacos:gateway-service-dev.yml
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml